$(document).ready(function(){
	/*var height = window.innerHeight;
	var w = $("#content");
	var subheight = w.position().top
	subheight += $("#foot").height()
	var newheight = height - subheight
	if(w.outerHeight(true) < newheight)
		w.height(newheight);
	*/
	
});
